name: Check Pull Request

on:
  pull_request:
    branches: [ main ]
    types: [opened, edited, synchronize, reopened]

jobs:
  trigger-workflow:
    uses: FraunhoferISST/dsp-github/.github/workflows/scan-pr-title.yml@main
